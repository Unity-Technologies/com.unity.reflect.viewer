namespace Unity.SpatialFramework.Handles
{
    /// <summary>
    ///   <para>How the tool handle or transform manipulator is oriented.</para>
    /// </summary>
    public enum PivotRotation
    {
        /// <summary>
        ///   <para>The tool handle or transform manipulator is oriented from the active object.</para>
        /// </summary>
        Local,
        /// <summary>
        ///   <para>The tool handle or transform manipulator is aligned along the global axes.</para>
        /// </summary>
        Global,
    }
}
